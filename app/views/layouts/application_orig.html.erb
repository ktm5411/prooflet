<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : 'Hide and Search Private Search Engine' %></title>
  <meta name="description" content="A private web search engine.">
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%= favicon_link_tag %>
  <%= stylesheet_link_tag "application", :media => "all", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= controller_name %>">
  <div id="layout">
    <div class="row">
      <div class="small-12 large-10 large-centered columns panel" id="search">
        <h1><%= link_to image_tag('logo.png', :alt => 'Hide and Search'), root_path %></h1>
        <%= render 'search/form' %>
      </div>
    </div>

    <%= yield %>
  </div>

  <div id="footer" class="clear">
    <%= render 'layouts/footer' %>
  </div>
  
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= include_gon %>
</body>
</html>
